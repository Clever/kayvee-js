routes:
  all-kv_middleware:
    matchers:
      via: [ "kayvee-middleware" ]
    output:
      type: "analytics"
      series: "requests.everything"
  rule-two:
    matchers:
      title: [ "foo-title" ]
    output:
      type: "analytics"
      series: "requests.everything"
